{
  admin off
  persist_config off
}

:{$HOPP_ADMIN_PORT:80} :3100 {
	handle_path /admin* {
		root * {$HOPP_SITE_ROOT:/site}/sh-admin-subpath-access
		file_server
		try_files {path} /
	}

	handle {
		respond 404
	}
}
